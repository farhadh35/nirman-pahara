name: nirman_pahara
description: "নির্মাণ পাহারা — Bangla civil works monitoring guide for non-engineers (Bangladesh)"
publish_to: 'none'
version: 2.0.2+6

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.3
  intl: ^0.20.2
  image_picker: ^1.2.0
  path_provider: ^2.1.5
  share_plus: ^12.0.0
  geolocator: ^14.0.2
  pdf: ^3.11.3
  file_picker: ^10.3.4
  excel: ^4.0.6
  archive: ^3.6.1
  xml: ^6.6.1

dev_dependencies:
  # Lets test/boq_import_flow_test.dart stand in for the file picker, so the
  # import screen can be driven the way a reader drives it.
  plugin_platform_interface: ^2.1.8
  # Used by test/report_photo_pixels_test.dart to write a photograph of a
  # known colour and then look for that colour in the rendered report.
  image: ^4.3.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/content/
    - assets/content/guide/
    - assets/content/guide/modules/
    - assets/content/checklists/
    - assets/content/lookups/
    - assets/content/rates/
    - assets/content/rules/
  fonts:
    - family: NotoSansBengali
      fonts:
        - asset: assets/fonts/NotoSansBengali-Regular.ttf
          weight: 400
        - asset: assets/fonts/NotoSansBengali-Medium.ttf
          weight: 500
        - asset: assets/fonts/NotoSansBengali-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/NotoSansBengali-Bold.ttf
          weight: 700
